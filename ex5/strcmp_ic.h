#ifndef STRCMP_IC_H // testa se o identificador "STRCMP_IC_H" NÃO está definido como uma macro
#define STRCMP_IC_H // se não estiver o pré-processador define-o, se já estiver salta diretamente para o #endif ()

int strcmp_ic(const char *str1, const char *str2);

#endif 
