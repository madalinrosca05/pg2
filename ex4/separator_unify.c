#include <stdio.h>
#include <limits.h>
#include <stdbool.h>
#include <ctype.h>
#include <string.h>

/*
 recebendo uma string no parâmetro str, aplica a formatação especificada e deixa a string processada
no início da memória anteriormente ocupada pela string original (note que a nova forma tem sempre
dimensão igual ou inferior à original.) São valorizadas as soluções que não necessitem de memória
temporária para o processamento. Pode realizar a modificação da string por manipulação direta ou usar
os mecanismos declarados nos header files normalizados, nomeadamente ctype.h e string.h.
*/

char formatacao

void separatorUnify( char str[] ){
    TEMP_SIZE == sizeof(str)
    formatacao()
    char temp[];

}